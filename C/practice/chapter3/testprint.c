#include<stdio.h>

int main() 
{
    float x, y; 
    int i; 

    printf("Enter the values for x, i, and y: ");
    scanf("%f%d%f", &x, &i, &y);

    printf("These are the three values: %f, %d and %f \n", x ,i,y);
    return 0; 

}